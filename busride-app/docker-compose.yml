services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: busride_sqlserver
    environment:
      # Secretos interpolados desde ./.env (ver .env.example); :? falla si faltan
      SA_PASSWORD: ${SA_PASSWORD:?Define SA_PASSWORD en busride-app/.env (ver .env.example)}
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
      - ./database/init:/docker-entrypoint-initdb.d
    networks:
      - busride_net
    healthcheck:
      # La imagen 2022-latest trae mssql-tools18 (no mssql-tools); -C confía en el certificado autofirmado.
      # $$SA_PASSWORD: doble $ para que compose no interpole y lo resuelva el shell del contenedor.
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -C -S localhost -U sa -P \"$$SA_PASSWORD\" -Q 'SELECT 1' || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  # One-shot (resuelve INF-1): la imagen mssql NO ejecuta /docker-entrypoint-initdb.d,
  # así que este servicio corre database/init.sh (01→02→03, idempotente) cuando
  # sqlserver está healthy y termina. El backend espera a que complete con éxito.
  # Uso manual: docker compose up sqlserver-init  (o run --rm sqlserver-init)
  sqlserver-init:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: busride_sqlserver_init
    depends_on:
      sqlserver:
        condition: service_healthy
    environment:
      DB_HOST: sqlserver
      SA_PASSWORD: ${SA_PASSWORD:?Define SA_PASSWORD en busride-app/.env (ver .env.example)}
    volumes:
      - ./database/init:/docker-entrypoint-initdb.d:ro
      - ./database/init.sh:/usr/local/bin/busride-init.sh:ro
    entrypoint: ["/bin/bash", "/usr/local/bin/busride-init.sh"]
    restart: "no"
    networks:
      - busride_net

  redis:
    image: redis:7-alpine
    container_name: busride_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - busride_net
    command: redis-server --appendonly yes

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      # El Dockerfile es multi-stage; este compose de desarrollo usa el target
      # development (start:dev + bind mount). Producción: docker-compose.prod.yml
      target: development
    container_name: busride_backend
    environment:
      NODE_ENV: development
      DB_HOST: sqlserver
      DB_PORT: 1433
      DB_NAME: busride_db
      DB_USER: sa
      DB_PASSWORD: ${SA_PASSWORD:?Define SA_PASSWORD en busride-app/.env (ver .env.example)}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:?Define JWT_SECRET en busride-app/.env (ver .env.example)}
      JWT_EXPIRES_IN: "24h"
      PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      sqlserver:
        condition: service_healthy
      sqlserver-init:
        condition: service_completed_successfully
      redis:
        condition: service_started
    networks:
      - busride_net
    volumes:
      - ./backend/src:/app/src
    command: npm run start:dev

volumes:
  sqlserver_data:
  redis_data:

networks:
  busride_net:
    driver: bridge
